# banking-realtime-warehouse

### Notes

- Airflow thì không cần tạo custom image trước vì đã có `airflow-base`
<!-- - Cần build custom image cho `debezium` trước khi chạy docker-compose vì không có image sẵn trên Docker Hub. -->